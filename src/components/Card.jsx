/* eslint-disable no-unused-vars */


// eslint-disable-next-line react/prop-types
const Card = ({phones}) => {
 
    return (
        <div>
            card
        </div>
    );
};

export default Card;